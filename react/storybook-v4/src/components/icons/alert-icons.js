import { flagIcon, warnIcon, checkmarkIcon, errorIcon, infoIcon } from './index';

const alertIcons = {
  default: flagIcon,
  warning: warnIcon,
  success: checkmarkIcon,
  error: errorIcon,
  info: infoIcon
};

export { alertIcons };
