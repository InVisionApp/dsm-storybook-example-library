import { flagIcon, warnIcon, checkmarkIcon, errorIcon, infoIcon } from './index';

const toastIcons = {
  default: flagIcon,
  warning: warnIcon,
  success: checkmarkIcon,
  error: errorIcon,
  info: infoIcon
};

export { toastIcons };
